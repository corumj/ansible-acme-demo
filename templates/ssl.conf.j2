server {
    listen 443 http2 ssl;

    server_name {{ domain }};


    ssl_certificate /etc/certs/{{ domain }}/signed.crt;
    ssl_certificate_key /etc/certs/{{ domain }}/domain.key;
    ssl_dhparam /etc/certs/{{ domain }}/combined.pem;
}